{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.databreeze.dev/contracts/v4/iam-scope-switch-command",
  "$comment": "IAM-028: closed workspace scope switch request. The server re-verifies membership coverage; the claim never authorizes.",
  "title": "IAM Scope Switch Command",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "workspaceId"],
  "properties": {
    "schemaVersion": { "const": 4 },
    "workspaceId": { "$ref": "https://schemas.databreeze.dev/contracts/v1/identifier" }
  }
}
